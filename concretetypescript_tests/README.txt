Together should_error and shouldnt_error represent the cases that can be called regressions if they fail. The other should_*_* are added not expected to work yet, unless by accident.
unit_tests/ contains a few runtime tests that should pass mostly, but contain a few known weird cases with switch statements.
