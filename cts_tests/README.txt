Use ./run.sh to run all tests.
Use 'SKIP_KNOWN_DEFECTS=1 ./run.sh' to get a lot less noise in output, and only check for regressions.
Use './run.sh --grep <pattern>' to only run matching tests (forwards arguments to Mocha).
